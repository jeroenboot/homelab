# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool: Default

steps:
- task: Docker@2
  inputs:
    command: 'build'
    Dockerfile: 'rpi-dotnet/Dockerfile'
    buildContext: 
    arguments: '-t jeroenboot/rpi-dotnet'